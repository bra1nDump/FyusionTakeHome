import SwiftUI

struct FyuseList: View {
    var body: some View {
        Text("FyuseList")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        FyuseList()
    }
}
